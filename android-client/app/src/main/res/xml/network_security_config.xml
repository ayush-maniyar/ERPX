<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext HTTP is permitted for local development only: the emulator's
         host loopback, and the private LAN ranges a home router hands out.
         Listing whole ranges (rather than one hardcoded address) means a new
         DHCP lease on the server machine does not require rebuilding the APK.
         Point the server URL at an https:// host for anything beyond local dev. -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator host loopback -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>

        <!-- Private IPv4 ranges (RFC 1918). Android matches these literally,
             so the common home-router /24s are enumerated below. -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>

    <!-- Fallback: allow cleartext generally in debug builds so any LAN address
         the user types works. Release builds keep the secure default. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
