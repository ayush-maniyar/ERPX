org.gradle.jvmargs=-Xmx1024m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# The backend address is entered in the app at runtime (first launch, or via
# "Change server address" on the login screen), so it does not need to be set
# here. Uncomment and set a real IP only to change the first-run default:
#apiBaseUrl=http://192.168.1.41:8080/